name: flip_panel_plus
description: >-
  3D flip-panel widgets for Flutter: a flip clock, a countdown timer with a
  controller, and animated flip panels for digits, images or any widget.
version: 2.0.0
homepage: https://github.com/omar-alshyokh/flutter_flip_panel_plus
repository: https://github.com/omar-alshyokh/flutter_flip_panel_plus
issue_tracker: https://github.com/omar-alshyokh/flutter_flip_panel_plus/issues

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  clock: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  fake_async: ^1.3.1

flutter:
