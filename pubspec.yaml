name: flip_panel_plus
description: A package for flip panel items like image, countdown or clock with built-in animation.
version: 1.0.0+3
homepage: https://github.com/omar-alshyokh/flutter_flip_panel_plus

environment:
  sdk: '>=2.19.1 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

