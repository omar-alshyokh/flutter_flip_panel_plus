/// The direction a [FlipPanelPlus] rotates when it flips to the next value.
enum FlipDirection {
  /// The panel folds upwards, like a departure board.
  up,

  /// The panel folds downwards. This is the default.
  down,
}
